name: CI

on:
  push:
  pull_request:

jobs:
  unit:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Print Swift
        run: swift --version || true
      - name: Run unit tests
        run: swift test -v
